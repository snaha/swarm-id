# Build outputs
dist/
demo/build/
swarm-id-build/

# Dependencies
node_modules/
demo/node_modules/
popup/node_modules/

# Lock files from non-root packages (root pnpm-lock.yaml is kept for workspace)
demo/pnpm-lock.yaml
popup/pnpm-lock.yaml

# Local settings
settings.local.json

# Multi-device integration test env (holds a real signer key — never commit)
lib/test/multi-device/.env

# Claude Code
.claude/worktrees/

# OS files
.DS_Store
Thumbs.db
