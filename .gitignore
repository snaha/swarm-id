# Build outputs
dist/
demo/build/

# Dependencies
node_modules/
demo/node_modules/
popup/node_modules/

# Lock files from non-root packages (root pnpm-lock.yaml is kept for workspace).
# `scripts/` is deliberately NOT here: it is outside the workspace, so nothing
# else pins its bee-js — its own lockfile is committed with it.
demo/pnpm-lock.yaml
popup/pnpm-lock.yaml

# Local settings
settings.local.json

# Live test suite env (holds a real signer key — never commit)
lib/test/live/.env

# Claude Code
.claude/worktrees/

# OS files
.DS_Store
Thumbs.db
