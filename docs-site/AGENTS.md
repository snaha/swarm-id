# Documentation Site (`docs-site/`)

Starlight (Astro) documentation website for the Swarm ID project. Pages are MDX files under
`src/content/docs/`; sidebar and site settings live in `astro.config.mjs`. Run from the repo
root: `pnpm dev:docs` / `pnpm build:docs` / `pnpm preview:docs`.
