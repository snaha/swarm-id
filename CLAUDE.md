@AGENTS.md

## Claude Code

Each package's rules live in its own `AGENTS.md`, imported here so they load at session start
rather than only when a file in that package happens to be read. Keep the rules where they
are — a rule copied up is a rule that goes stale.

@lib/AGENTS.md
@ui/AGENTS.md
@docs-site/AGENTS.md
